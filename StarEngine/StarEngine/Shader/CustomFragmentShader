#version 330 core
out vec4 FragColor;

in vec3 ourNormal;
in vec2 TexCoord;

uniform sampler2D texture1;

void main()
{

	FragColor = texture(texture1, TexCoord)* clamp(dot(vec3(1,0,1),ourNormal),0.4,1) ;
}